<!-- example.launch.xml -->

<launch>

  <!-- args that can be set from the command line or a default will be used -->
  <arg name="obstacle" default="1.0"/>
  <arg name="degrees" default="90.0"/>

  <!-- start pre_approach node -->
  <node pkg="attach_shelf" exec="pre_approach_node" name="pre_approach" output="screen">
    <param name="obstacle" value="$(var 'obstacle')"/>
    <param name="degrees" value="$(var 'degrees')"/>
  </node>

  <!-- start rviz -->
  <node pkg="rviz2" exec="rviz2" name="rviz2" args="-d /home/user/ros2_ws/src/checkpoint9/attach_shelf/config/cp9_config.rviz">
    <param name="use_sim_time" value="True"/>
  </node>

</launch>